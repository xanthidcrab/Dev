<Window x:Class="TaskDeneme.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskDeneme"
        xmlns:vm="clr-namespace:TaskDeneme.ViewModel"
        xmlns:vaalidater="clr-namespace:TaskDeneme.Validations"
        xmlns:behav="clr-namespace:TaskDeneme.Behaviours"
        d:DataContext="{d:DesignInstance vm:ControlViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError"
                         Value="True">
                    <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={RelativeSource Self},
                                    Path=(Validation.Errors)[0].ErrorContent}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DataGrid ItemsSource="{Binding AxisList}" SelectedItem="{Binding SelectedAxis}"/>
        <TextBox Grid.Row="1" Text="{Binding Number, UpdateSourceTrigger=PropertyChanged}"
                 behav:NumericOnlyBehavior.IsEnabled="True"
                 >
             
        </TextBox>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="Start"
                    Command="{Binding StartCommand}"
                    Margin="5" />
            <Button Grid.Column="1"
                    Content="Stop"
                    Command="{Binding StopCommand}"
                    Margin="5" />
        </Grid>
    </Grid>
</Window>
