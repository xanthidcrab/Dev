<Window x:Class="LangExamples.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LangExamples"
        xmlns:viewmodel="clr-namespace:LangExamples.MVVM.ViewModel"
        d:DataContext="{d:DesignInstance Type=viewmodel:UILanguageViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ComboBox 
                  HorizontalAlignment="Left"
                  VerticalAlignment="Top"
                  Width="200"
                  Margin="10"
                  ItemsSource="{Binding Cultures}"
                  SelectedItem="{Binding SelectedItem, Mode=TwoWay}" />
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"
                       Text="{Binding HelloWorld}"
                       Margin="10" />
            <TextBlock Grid.Row="1"
                       Text="{Binding HelloWorld2}"
                       Margin="10" />
            <TextBlock Grid.Row="2"
                       Text="{Binding HelloWorld3}"
                       Margin="10" />
            <TextBlock Grid.Row="3"
                       Text="{Binding HelloWorld4}"
                       Margin="10" />
            <TextBlock Grid.Row="4"
                       Text="{Binding HelloWorld5}"
                       Margin="10" />
            <TextBlock Grid.Row="5"
                       Text="{Binding HelloWorld6}"
                       Margin="10" />
            <TextBlock Grid.Row="6"
                       Text="{Binding HelloWorld7}"
                       Margin="10" />
            
        </Grid>
        
    </Grid>
</Window>
