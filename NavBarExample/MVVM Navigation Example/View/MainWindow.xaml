<Window x:Class="MVVM_Navigation_Example.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVM_Navigation_Example"
        xmlns:viewmodel="clr-namespace:MVVM_Navigation_Example.ViewModel"
        d:DataContext="{d:DesignInstance Type=viewmodel:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <DockPanel LastChildFill="True">

            <!-- Üst Menü: Navigation Butonları -->
            <StackPanel Orientation="Horizontal"
                        DockPanel.Dock="Top"
                        Background="LightGray"
                        >
                <Button Content="Auto"
                        Command="{Binding NavigateAutoCommand}"
                        Margin="5" />
                <Button Content="Manual"
                        Command="{Binding NavigateManualCommand}"
                        Margin="5" />
                <Button Content="Settings"
                        Command="{Binding NavigateSettingsCommand}"
                        Margin="5" />
            </StackPanel>

            <!-- Sayfa İçeriği -->
            <ContentControl Content="{Binding CurrentViewModel}" />
        </DockPanel>
    </Grid>
</Window>
